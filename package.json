{
  "name": "clubalmanac-v2",
  "version": "0.0.1",
  "private": true,
  "description": "Clubalmanac — Convex backend (migratie van AWS).",
  "type": "module",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:codegen": "convex codegen",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^25.6.0",
    "convex-test": "^0.0.50",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "convex": "^1.36.1",
    "exif-parser": "^0.1.12"
  }
}
