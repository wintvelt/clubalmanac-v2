# Audit track record

Tussen 2026-04 en 2026-06 hebben 18 audit-cycli op clubalmanac-v2 backend de volgende productie-bugs ontdekt en gefixt vóór cutover.

## 10 productie-bugs gevonden

1. **U8** (audit-2): user-delete miste M2-cascade (founder/admin succession + group cleanup) — pure record-delete zonder downstream effect
2. **AP4** (audit-4): album-photo delete miste group-cover cleanup — dangling cover-refs mogelijk
3. **requireWebmaster case-sensitivity** (audit-7): Clerk normaliseert email naar lowercase, env-var `Wouter@me.com` lockt webmaster uit
4. **features.update/remove RBAC drift** (audit-7): submitter-only ipv webmaster-only conform plan
5. **convex/http.ts ontbrak** (audit-8): IB1 bounce-handler in productie dood — webhook endpoint niet aangemaakt
6. **users.ts email normalization** (audit-8): mixed-case duplicates + invite-gate failures
7. **decline order-bug** (audit-8): idempotency werkte niet bij verkeerde caller
8. **CLUBALMANAC_APP_URL silent prod-fallback** (WP5-audit S-3): dev-deployment zonder env-var → invite-links naar prod-host → 404 bij klik. Fix: fail-loud throw bij unset.
9. **CLUBALMANAC_STAGE silent "dev"-fallback** (WP5-audit S-3): prod-deployment zonder env-var → problem-reports met label "dev" → urgency-mis-routing. Fix: fail-loud throw bij unset.
10. **WP8 EXIF-arithmetiek 5↔7 verwisseling** (WP8-audit B1, resolved 2026-06-03): `convex/lib/exifOrientation.ts` ROT90/ROT270/FLIP-tabellen hadden oriëntaties 5 (transpose) en 7 (transverse) verwisseld t.o.v. canonieke EXIF. Bereikbaar via `flipY`-feature (1→2 canoniek, daarna rotate 90 trapt erin) en via gespiegelde uploads (extractie leest tags.Orientation 1:1 uit exif-parser, dus front-camera-bestanden komen canoniek binnen en zouden onder rotate vergiftigd worden). Tests vingen 't niet omdat A's testbestand en B's impl dezelfde uit-de-spec-gekopieerde tabel deelden — circulaire validatie. Fix: A herleidde tabellen onafhankelijk via pixel-array-oracle op asymmetrisch 2×3 raster (commit `e79cb1d`), pinde gespiegelde-start-rotaties + flipY-trap tegen externe oracle; B paste impl aan (commit `ac405b7`); audit re-run bevestigde oracle daadwerkelijk onafhankelijk + 99/99 groen.

## Plus

- Dead code geëlimineerd (U9)
- Test-coverage substantieel uitgebreid
- Design-doc en code synchroon gemaakt
- Comment-bias-patronen weggepoetst
- Reservation pattern voor uploads (cyclus 1 architectuur-rewrite)
- EXIF/geocoding hardening + Photon switch (cyclus 2)
- WP5 (Mailjet): verified-sender hard gate fail-closed, Bearer-auth webhook, NL-templates 1:1 oude SES, PII-guard structureel via template-signatures
- WP5-audit follow-up afgesloten 2026-05-18 (commit `9654f81`): webhook-auth strict-equality regression-guards (S-1), end-to-end replay-test (S-2), STAGE+APP_URL fail-loud (S-3), Mailjet-creds fail-fast vóór fetch (N-1). 445 tests groen, CI groen, geen blockers.
- **WP5 deferred naar pre-cutover** (uit oorspronkelijke WP5-spec, niet in deze cyclus): integration-tests `tests/integration/mailjet/sendRoundtrip.test.ts` (niet in CI), en twee empirische mens-gates — Gate 1 send-roundtrip naar test-inbox (verified-sender silent-failure proof), Gate 2 bounce via Mailjet dashboard Event API "Test event"-knop met 4-punts verificatie. Wouter draait beide handmatig vóór cutover.
- **WP5 Gate 1 + Gate 2 op dev gepasseerd 2026-05-18**: end-to-end-roundtrip succesvol. Gate 2 via echte-bounce (invite naar `wintvelt-to-fail-again@me.com`, iCloud SMTP-reject, Mailjet bounce-pipeline ~10min, webhook → `inviteBounceEvents` → invite gepatcht naar `expired` + `bouncedAt` → bounced-notify-mail in inbox). Replay-test via curl met zelfde MessageID = 200 + geen tweede rij (dedup-discipline werkt end-to-end via webhook-laag, niet alleen handleBounce-niveau). Tijdens setup drie bugs opgespoord + gefixed: (1) Mailjet dashboard ondersteunt geen custom-headers → switched naar Basic-auth-in-URL (commit `3c8b0dc`); (2) Convex `dev --once` had niet recent gedraaid → http-routes liepen achter (geen code-fix, alleen workflow-leerpunt); (3) Convex EU HTTP-routes vereisen region-suffix `<deployment>.eu-west-1.convex.site`, eerdere docs hadden de no-region variant → 404 op webhook tot URL-fix (commit `b68da68`).
- **WP6 (Clerk session-webhook + atomic onboarding) afgesloten 2026-05-18**: `/clerk-webhook` httpAction met Svix HMAC-verify, atomic `internal.users.registerFromSession` (insert users + auto-accept pending invites + memberships in één tx). Publieke `api.users.register` verwijderd; 32 test-files mechanisch gemigreerd via `tests/_helpers/auth.ts` helper-bypass (B koos directe `ctx.db.insert` om auto-accept-side-effect te isoleren — bewuste deviatie van spec §test-migratie-strategie, gedocumenteerd in spec audit-follow-up). Audit vond 2 doc-blockers (cascade-matrix S1 niet bijgewerkt, external-services Clerk-tabel ontbrak) + 1 helper-strategie-deviatie + integration-test-deferral, allen post-audit door regie afgesloten zonder code-cyclus. Mini-fix N-6 (defense-in-depth membership-check in registerFromSession) + N-7 (recordVisit helper-consistency) meegelopen.
- **WP6 deferred naar pre-cutover** (uit WP6-spec audit-follow-up): integration-test `tests/integration/clerk/onboardingWebhook.test.ts` — blijft deferred. Empirische mens-gates wél direct gedraaid (zie hieronder).
- **WP6 Gates 1+2+3 op dev gepasseerd 2026-05-18**: alle drie atomic-onboarding-flow-paden empirisch bewezen via Clerk Account Portal hosted signup-UI, geen frontend nodig. Gate 1 (happy-path met groupId-invite): users-row + invite-accept + membership in één tx (zelfde createdAt-timestamp 1779133842429). Gate 2 (idempotency re-login): twee re-logins op twee verschillende users → géén duplicate users-rows, géén duplicate memberships, subject-lookup-no-op werkt. Gate 3 (zero-invite fallback): signup met email zonder pending invite → users-row aangemaakt, géén membership = "registered-no-membership" terminal werkt. Tijdens setup één bug-vondst: Clerk Account Portal URL is `https://<instance>.accounts.dev` (geen `clerk.` ertussen), eerdere docs hadden de verkeerde host → runbook gefixed in commit `b104f02` later opnieuw aangepast.
- **WP7 upload-gate op dev gepasseerd 2026-05-18** als integration-test (`tests/integration/uploads/uploadRoundtrip.test.ts`) — end-to-end POST /upload → reservation → storage → scheduled extractMetadata → Photon reverseGeocode → locationLabel op echte iPhone JPEG met EXIF + GPS (Tbilisi-coords). **Twee productie-bugs gevonden via deze gate** (waarom integration-gates op echte runtime nodig zijn naast unit-tests):
  1. `convex/photos.ts:379` `blob.slice(0, 12).arrayBuffer()` → RangeError in Convex isolate-runtime (unit-tests met mock-blob raakten dit nooit). Fix: full buffer eerst laden, dan in-memory Uint8Array-subarray voor HEIC-sniff.
  2. `convex/photos.ts:403` `Buffer.from(...)` → ReferenceError in Convex isolate-runtime (Buffer ontbreekt). Fix: `extractMetadata` verhuisd naar nieuwe file `convex/photoMetadata.ts` met `"use node";` directive die wel Buffer-global heeft. Scheduler-call van `internal.photos.extractMetadata` naar `internal.photoMetadata.extractMetadata`. 25 unit-test-refs mass-renamed. Patch + lookup-helpers + reverseGeocode blijven in isolate-runtime `photos.ts`.

  Beide bugs waren in de cyclus-1+2 upload-impl gepasseerd door alle audits + 467 unit-tests, want unit-tests mocken `ctx.storage.get` en `exif-parser` waardoor de runtime-mismatches niet getriggerd werden. Bevestigt recurring-pattern: **integration-gates op echte runtime vangen runtime-bugs die unit-tests structureel niet kunnen vangen** — discipline-rationale voor de empirische-gate-aanpak.
- **WP11 (Deferred integration-tests Mailjet + Clerk) afgesloten 2026-06-21**: `tests/integration/mailjet/sendRoundtrip.test.ts` pin't Mailjet Send-API direct (silent-failure-pin bij onverified From + happy-path met MessageID); `tests/integration/clerk/onboardingWebhook.test.ts` pin't synth-payload met fresh subject + echte Svix-HMAC + atomic onboarding via drie-velden-timestamp-pin (`users.createdAt`/`invites.respondedAt`/`memberships.joinedAt`). Split-workflow: Mailjet A+audit (geen B), Clerk A→B→audit. A's spec-criticus-pass vond vier blokkers in regie's draft: (1) invariant 1 pin'de niet-bestaand REST-mechanisme (de WP5-gate is een pure env-var-check `isSenderVerified`), (2) DB-verificatie + cleanup voor fresh subject vereist nieuwe `convex/_test.ts`-helpers (triggert B-fase per integration-tests.md §Test-only-functions), (3) capture-and-replay payload botst met fresh-subject-eis en raakt verouderd zonder detectie (vervangen door synth-but-faithful), (4) accuratesse-correctie: atomic-pin gebeurt op drie verschillende velden, niet één `createdAt`. A breidde helper-set uit van 4 → 6 (`seedOnboardingFixture` + `cleanupOnboardingSeed`) — anders niet pinbaar zonder seeded pending state; expliciet geflagd vóór impl-start per scope-uitbreidings-discipline. B implementeerde de 6 helpers in `convex/_test.ts` met INTEGRATION_TEST_ENABLED-gate (WP2-precedent), cleanup-volgorde respecteert FK-orphans. Audit groen: geen blockers; twee should-fixes in closeout afgehandeld door regie (DRY-cleanup `getConvexSiteBase` over alle drie integration-tests, invariant-10 scope-correctie naar Convex-rakende tests). 609 unit-tests groen, beide integration-files skippen luid zonder secrets, typecheck clean. Fase 2 hiermee AFGEROND.
- **WP10 (Integrity-check / monitoring) afgesloten 2026-06-04**: `internal.monitoring.integrityCheck` daily 04:30 UTC scant in één transactie vier categorieën (storage-orphans, aggregate-drift, FK-integriteit, geen self-healing). Alert via `sendMonitoringAlert`-action met dedup via nieuwe `monitoringRuns`-tabel + maandelijkse heartbeat. A vond drie blind spots in regie's draft: (1) storage-orphan-scope miste `users.profilePhotoStorageId` (zonder fix had elke profielfoto vals-positief gegeven), (2) `ratingAverage` is een float — `≥1`-drempel zou Δ0.5-drift missen, gefixt naar epsilon-mismatch + handberekende oracle (geen recompute-vs-recompute, ab-audit anti-pattern correct toegepast vóór bug kon ontstaan), (3) FK-lijst van 10 indicatief naar 24 volledig via schema-scan met verplicht/optioneel-split. Plus architectuur-correctie: mythische `sendEmail`-action vervangen door `sendProblemReport`-patroon. Audit-cyclus 1 vond S-1 (dedup-semantiek-gap: drift→clean→drift werd niet gere-alarmeerd, recurrence-blind-spot in beide spec-lezingen) en N-1 (heartbeat-tekst misleidend bij gededupte drift). Regie koos strict-consecutive semantiek (vergelijk met direct voorgaande run); A revideerde spec+tests, B revideerde impl, audit re-run groen (één doc-deliverable-drift D-1 op heartbeat-rij in external-services.md in closeout meegelopen). 30 tests groen, suite 609, typecheck clean.
- **WP9 (IB2 natural-expiry cron) afgesloten 2026-06-03**: `internal.invites.expirePendingInvites` internalMutation + daily cron `"expire pending invites"` 04:00 UTC. Patcht `status="pending"` + `expiresAt <= now` → `status="expired"`. Geen email-action, geen `bouncedAt`/`respondedAt`-write — stille expiry. Natural-expiry-fingerprint = `expired` ∧ geen `bouncedAt` ∧ geen `respondedAt` (drie terminal-herkomsten onderscheidbaar: natural / bounce / user-decline). A-rol vond twee blind spots in regie's draft: invariant 8 (respondedAt-symmetrie met bouncedAt voor system-events) en cascade-matrix cat-correctie (3 → n.v.t. — single-row state-transition, geen cascade). Audit groen — geen blockers, geen should-fix. Twee nice-to-haves: spec-tekst noemde niet-bestaande `bounced`-status (gefixt in closeout, schema-union is `pending|accepted|declined|expired`) en auth-boundary test (overkill voor `internalMutation`, niet geadresseerd). 15 tests groen, suite 578, typecheck clean.
- **WP8 (Photo rotation, EXIF-only) afgesloten 2026-06-03**: `photos.rotate` mutation in `convex/photos.ts` + `convex/lib/exifOrientation.ts` helper met 8-staat-arithmetiek-tabellen (ROT0/90/180/270 + FLIP). Auth-pad volgt A9 exact: owner OR group-admin via `albumPhotos.by_photo` → unieke `groupId`s → `memberships.by_user_and_group` met `role === "admin"`. Atomair patch: `exifOrientation` + (conditioneel) `width`/`height`-swap bij 90°/270°. Geen sharp, geen scheduled action, geen `"use node"`-file — spec revisie cyclus-2 maakte pixel-rotatie redundant (DB-`exifOrientation` is bron-van-waarheid, client past CSS-transform toe per Phase-4 contract). Audit-cyclus-1 vond bug #10 (5↔7-verwisseling, zie boven); A's pixel-array-oracle als externe verifier blokkeerde een tweede passage van het anti-pattern. Twee learnings gepromoveerd in commit `6ef4f09`: "gedeelde-lookup-tabel blind spot" → `ab-audit-workflow.md` §Anti-pattern, en "intent over delta" → `prompt-discipline.md`. 99 rotate-tests groen, suite 566, typecheck clean.

- **WP12 (Data-migratie tooling DynamoDB + S3 → Convex) tooling afgerond 2026-08-23**: acht commando's in `scripts/migrate/` (`extract`/`inspect`/`transform`/`load-files`/`load-records`/`verify`/`prune-storage`/`reset`) plus `convex/migration.ts`. Mechanisme-keuze empirisch beslist: een zelfgebouwde snapshot-zip importeren faalt op `invalid _id`, dus API-import waarbij Convex de ID's mint. **Vier audit-rondes, drie fix-cycli.** A's spec-criticus-pass vond zes semantische afwijkingen tussen bron en doelschema, waarvan de zwaarste — `seenPics` is een **ongelezen**-wachtrij, geen gelezen-historie — ook fout in `migratie-plan-convex.md` §Unread-count stond; het gedocumenteerde algoritme zou elke user na cutover nul ongelezen foto's geven, precies het omgekeerde van de oude app. Twee productie-bugs vóór eerste gebruik gevangen: (1) `load-records` gooide bij een ontbrekende storage-map stilzwijgend álle foto's weg terwijl `verify` groen meldde, omdat `verify` zijn verwachting berekende met dezelfde `applyStorageMap` die de fout maakte; (2) `prune-storage` kon de volledige T-2-upload wissen doordat de beveiliging een tellingsvergelijking was die triviaal waar is zodra beide kanten nul zijn — bereikbaar via een `extract` tegen de verkeerde tabel, waarna `verify` zélf adviseert het commando te draaien. Empirische correcties op het plan: 20 users in plaats van 16, 5,6 GB in plaats van 8,3 GB, en 10 video's van 19 GB in een aparte bucket zonder DynamoDB-record (buiten scope gehouden). Spec bij closeout teruggebracht van 1020 naar 328 regels. 764 tests groen, typecheck clean. Integration-test uitgesteld conform WP5/WP6/WP11-precedent; dev-seed-run dekt hem empirisch af.

## Recurring-pattern: doc-deliverable-drift bij codecomplete-WPs

WP5-audit (CLUBALMANAC_APP_URL/STAGE runbook-fix achteraf) en WP6-audit (cascade-matrix S1, external-services Clerk-tabel) flagden hetzelfde gat: spec-genoemde doc-deliverables liepen achter op de impl-commit. Gepromoveerd naar standing rule in [`commit-discipline.md`](./commit-discipline.md) §Doc-deliverable-checklist: B's commit-message moet expliciet de spec-genoemde doc-deliverables co-committen of een marker geven waarom niet. Audit toetst dit voortaan als should-fix.

## Recurring-pattern: gedeelde bron tussen controle en gecontroleerde

WP8-audit (EXIF-arithmetiek 5↔7) liet zien dat wanneer A een waarheidstabel uit de spec in de test plaatst en B die in de impl her-codeert, A→B alleen interne consistentie valideert. Een wrong-but-self-consistent tabel passeert alle tests omdat test én impl dezelfde (afwijkende) bron delen. Delta-pins (90∘90=180, inverse) zijn hiervoor blind: groep-structuur-behoudende verwisselingen (zoals 5↔7) overleven ze. Gepromoveerd naar standing rule in [`ab-audit-workflow.md`](./ab-audit-workflow.md) §Anti-pattern: gedeelde bron tussen controle en gecontroleerde: bij arithmetiek of lookup-tabellen moet de test minstens enkele cellen tegen een *onafhankelijke* oracle pinnen (eerste-principes-afleiding of tweede, niet-spec-gekopieerde referentie), niet alleen tegen een tabel die uit dezelfde spec-bron komt als de impl.

## Recurring-pattern: env-var-runbook-gap

Twee audits op rij (WP4 `INTEGRATION_TEST_ENABLED`, WP5 vijf nieuwe env-vars) wezen op zelfde gat: nieuwe env-vars werden in code geïntroduceerd zonder runbook-entry, met silent fallbacks die foutgevoelig bleken. Gepromoveerd naar standing rule in [`work-package-specs.md` §Ops-runbook-impact](./work-package-specs.md) en als verplicht risico-veld in [`_template.md`](../work-packages/_template.md). Default-fallbacks worden afgewezen tenzij hard gemotiveerd of fail-loud.

## Backlog (uit audits, niet meegelopen in eigen WP)

- **WP5 N-2**: `invite.email` lowercase doorgegeven aan bounce-notify-template (origineel "Bob@X.com" wordt "bob@x.com"). UX-detail. Past in future "email-template polish" mini-WP indien ooit gewenst.
- **WP5 N-3**: `console.log` free-form structurering in graceful-skip-paths. Niet grep-baar. Past in cross-cutting observability/logging-werkpakket (zie [`migratie-plan-convex.md` §Monitoring & backup](../migratie-plan-convex.md)).

## Wanneer twijfel: doe de audit

Wanneer Wouter twijfelt of A→B + audit voor een werkpakket de moeite is: ja. Alle audits hebben minstens iets opgeleverd, vaak meer dan verwacht.

Voor 16-user app met hard cutover (geen parallel draaien) is pre-cutover bug-vangst cruciaal. Discipline werkt aantoonbaar.

## Recurring-pattern detection

Elke ~5 audit-cycli (of na een grote vondst): scan deze log + de bug-lijst hierboven op herhalend patroon. Zaken die twee of meer audits independent flaggen zijn kandidaat voor promotie naar een standing rule in een van de conventions — meestal [`prompt-discipline.md`](./prompt-discipline.md), [`ab-audit-workflow.md`](./ab-audit-workflow.md), of een nieuwe convention waar het patroon zinvol thuishoort. Voorbeelden van wat een patroon kan zijn: een type vondst dat audits steeds te laat vinden, een gat dat tests systematisch missen, of een prompt-stijl die bias inbouwt.

Het doel is dat de audit-pas zelf goedkoper wordt — niet door minder grondig te zijn, maar door het herhalend werk uit de audit-output naar de impl-prompt te trekken (waar 't preventief werkt).

## Cross-cutting gaps

Per-werkpakket A→B→audit vangt functional correctness en security-per-surface uitstekend. Maar accessibility, GDPR-lifecycle, deployment-headers en architectuur-niveau duplicatie passen niet in één werkpakket en blijven structureel buiten beeld. Vóór cutover (of een latere uitbreiding van user-set) draait daarom een [cross-cutting review](./cross-cutting-review.md): vier verse perspectief-reviews in parallel die deze laag dichten.

## Recurring-pattern: afgeleide verwachting (WP12)

WP12 leverde twee verse instanties van hetzelfde patroon, één laag hoger dan WP8: een verificatiestap die zijn verwachting berekent met dezelfde functie die de te controleren stap gebruikt, en een fixture die zijn testdata bouwt met de productiefunctie. In beide gevallen is een defect voor de hele suite onzichtbaar. Samengevoegd met het WP8-patroon tot één regel in [`ab-audit-workflow.md`](./ab-audit-workflow.md) §Anti-pattern: gedeelde bron tussen controle en gecontroleerde, met een toets die de auditor uitvoert in plaats van beoordeelt: **schakel de bescherming uit die de tekst belooft en kijk of er iets omvalt.** In WP12 leverde die toets vier bevindingen op rij op, waarvan geen enkele uit lezen alleen zichtbaar was — de code klopte telkens, er hield alleen niets hem vast. Sinds ronde 4 is de mutatie-check een vaste auditstap bij elke nieuw toegevoegde bescherming.
